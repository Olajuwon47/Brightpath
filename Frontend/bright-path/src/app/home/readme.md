# Page Rendering Guide

- Import all homepage sections from `components/home/` into `app/home/page.tsx`.
- Use these imports to render the full homepage layout in the browser from the `app/home/page.tsx`.
